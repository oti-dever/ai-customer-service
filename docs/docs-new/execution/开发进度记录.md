# 开发进度记录

本文档记录 YY AI 客服桌面工作台项目的开发进度。每次完成有意义的改动后更新此文档。

---

## 当前状态概览

| 阶段 | 状态 | 说明 |
|------|------|------|
| M0 工程骨架 | ✅ 完成 | 项目结构已建立 |
| M1 统一模型 | ✅ 完成 | `Models::*` 已定义，旧模型兼容层已建立 |
| M2 Qt 主流程 | ✅ 完成 | Model/View 架构已收敛 |
| M2.5 模块边界重构 | ✅ 完成 | IPC 层、RPA 服务层已建立，MessageRouter 已收敛 |
| M3 状态机与缓存 | ✅ 基本完成 | 消息状态流转、草稿保存、最近会话恢复和会话状态流转已完成 |
| M4 Python AI | ✅ 基本完成 | Python sidecar、健康检查、AI 建议接口、聚合工作台入口和进程管理已打通 |
| M5 单平台 PoC | ⏳ 待做 | - |
| M6 MVP 验收 | ⏳ 待做 | - |

---

## 进度日志

### 2026-06-04（P1：客户端滚动条统一与丝滑滚动）

统一客户端滚动区域体验，贴近聊天区的轻量滚动效果：

1. **全局滚动条样式**
   - `ApplyStyle::globalScrollBarStyle()` 输出统一悬浮式滚动条 QSS
   - 滚动条轨道透明、无箭头，滑块为胶囊形圆角矩形
   - 主应用和 AI 客服后台独立窗口均接入同一份滚动条样式

2. **滚动行为**
   - 新增 `ScrollBehavior` 全局事件过滤器，启动时自动管理所有 `QAbstractScrollArea`
   - 鼠标进入滚动区域时滚动条淡入，离开或滚轮空闲后自动淡出
   - 滚轮改为按像素动画滚动，避免列表/聊天区一次滚动跨度过大

3. **验证**
   - `ReadLints` 检查相关 C++/CMake 文件：无新增诊断
   - 已通过 `git diff --check`

### 2026-06-04（P1：微信消息角色判定主路径修复）

修复微信未读消息进入聚合界面时右侧我方消息被标成客户消息的问题：

1. **Python 角色判定**
   - `safe_rect_tuple()` 支持读取 `WechatUiaMessageSample.left/top/right/bottom`
   - 微信主采样路径 `_attach_roles_to_samples()` 改为调用 `judge_message_role()`
   - sample 坐标现在可用于截图裁剪，颜色方差法可参与主路径判定；失败后继续用位置法兜底

2. **验证**
   - 补充 sample 坐标位置判定和主路径角色判定测试
   - 已通过：
     - `python -m unittest tests.test_wechat_role_and_send`
     - `python -m unittest tests.test_wechat_sidecar_adapter tests.test_wechat_role_and_send`
     - `python -m py_compile python/rpa/platforms/wechat/uia_scoring.py python/rpa/platforms/wechat/screenshot.py python/rpa/platforms/wechat/reader.py python/rpa/platforms/wechat/role_judgement.py`

### 2026-06-03（P1：消息方向与角色合同收口）

按“Python 负责最终判定，C++ 不再理解平台细节”的方向，收口微信/千牛消息左右气泡判定边界：

1. **协议边界**
   - Python 平台 reader/adapter 输出统一 `sender_role`
     - `customer`：客户消息
     - `agent`：我方客服消息
     - `system`：系统消息
   - Python 同时保留 `direction=inbound|outbound|system`，供 C++ 内部未读、发送状态、AI 上下文使用

2. **C++ 收口**
   - `WechatRPAAdapter` / `QianniuRPAAdapter` 的方向归一逻辑改为优先信任 `sender_role`
   - 只保留 `direction=inbound|outbound|system|in|out` 的少量兼容兜底
   - 去掉 C++ 对 `assistant/me/self/outgoing/received` 等宽泛平台别名的判断，避免展示层继续猜平台细节

3. **测试与验证**
   - 补充微信/千牛 sidecar 测试断言，确认事件 payload 含统一 `direction` 与 `sender_role`
   - 已通过：
     - `python -m unittest tests.test_wechat_sidecar_adapter tests.test_qianniu_sidecar_adapter`
     - `yy_ai_customer_service` Debug 构建

### 2026-06-03（P1：千牛监听线程 UIAutomation 初始化修复）

联调千牛“开始监听”时，Python observer 线程访问 `uiautomation/comtypes` 报 `[WinError -2147221008] 尚未调用 CoInitialize`。本轮补齐千牛侧 UIA 线程初始化保护：

1. **线程初始化**
   - `python/rpa/platforms/qianniu/uia.py` 新增 `uia_guard()`
   - 每次进入 UIAutomation 调用前执行 `comtypes.CoInitialize()`
   - 退出时执行 `comtypes.CoUninitialize()`
   - 增加千牛 UIA 全局锁，避免 observer 与命令线程并发操作 UIA

2. **调用覆盖**
   - `QianniuSidecarAdapter.command()` 已用 `uia_guard()` 包住 `connect`、`health_check`、读取、发送等命令
   - `QianniuSidecarAdapter._observer_loop()` 已用 `uia_guard("qianniu_observer")` 包住每次轮询扫描

3. **验证**
   - `ReadLints` 检查千牛相关 Python 文件：无新增诊断
   - 已通过：
     - `python -m py_compile python/rpa/platforms/qianniu/uia.py python/rpa/platforms/qianniu/adapter.py`
     - `python -m unittest tests.test_qianniu_sidecar_adapter`

### 2026-06-03（P1：平台列表与监听状态查询）

继续推进 P1 交互模型，把“UI 写死平台入口”向“Python 服务端声明可用平台和监听状态”收口。

1. **Python 平台状态接口**
   - `RpaBridge.platforms()` 返回微信、千牛的平台注册状态、连接状态、监听状态和 observer 运行状态
   - 新增 `GET /api/platforms`
   - 兼容新增 `GET /api/platform/list`

2. **C++ 状态查询**
   - `IpcService::fetchPlatformStatuses()` 接入 `/api/platforms`
   - 聚合工作台启动后、服务状态变化后、开始/停止监听后都会刷新平台状态

3. **UI 状态区分**
   - Python 服务未连接时禁用平台监听控件，并显示“Python 服务未连接”
   - 服务可用后按平台注册状态启用微信/千牛复选项
   - 状态文案区分“暂无已注册平台”“未监听平台”“监听中：微信/千牛”

4. **验证**
   - `ReadLints` 检查相关 Python/C++ 文件：无新增诊断
   - 已通过 Python 单元测试：
     - `tests.test_service_server_platform_routes`
   - 已在 `build/Desktop_Qt_6_10_2_MSVC2022_64bit-Debug` 下重新编译并通过：
     - `yy_ai_customer_service`

### 2026-06-03（P1：平台监听改为用户显式启动）

落实 P1 交互模型第一小步：客户端启动后只注册平台 adapter，不再默认让微信和千牛进入监听。

1. **启动行为**
   - `PlatformBootstrap` 现在只注册 `simulator` / `qianniu` / `wechat` adapter
   - 默认不再调用 `connectPlatform()` / `startListening()`

2. **UI 入口**
   - 聚合工作台左侧新增“平台监听”区域
   - 支持勾选微信、千牛后点击“开始监听”
   - 支持对选中平台点击“停止监听”
   - UI 状态区区分“未监听平台”和“监听中：微信/千牛”

3. **平台命令**
   - `ConversationManager` 新增按平台 start/stop/listening 状态的薄封装
   - `WechatRPAAdapter` / `QianniuRPAAdapter` 只有 Python `connect` 命令成功后才标记为已监听
   - 停止监听会向 Python 发送 `disconnect`，由 Python 停止对应 observer

4. **验证**
   - `ReadLints` 检查相关 C++ 文件：无新增诊断
   - 已在 `build/Desktop_Qt_6_10_2_MSVC2022_64bit-Debug` 下重新编译并通过：
     - `yy_ai_customer_service`

### 2026-06-03（P1：platform HTTP 语义路径）

继续推进 P1 命名壳收口，在保留旧 `/api/rpa/*` 兼容面的同时，为 Python sidecar 增加 `/api/platform/*` 主语义路径：

1. **Python 服务端路由**
   - 新增 `GET /api/platform/health`
   - 新增 `GET /api/platform/events`
   - 新增 `GET /api/platform/replay`
   - 新增 `POST /api/platform/command`
   - 旧 `/api/rpa/health|events|replay|command` 继续可用

2. **C++ 主调用迁移**
   - `IpcService::fetchPlatformReplay()` 已从 `/api/rpa/replay` 切到 `/api/platform/replay`
   - 聚合 UI 日志从 `rpa replay backfill` 调整为 `platform replay backfill`
   - `python/service/README.md` 以 `/api/platform/*` 作为主接口说明，旧 `/api/rpa/*` 标为兼容路径

3. **验证**
   - `ReadLints` 检查相关 Python/C++ 文件：无新增诊断
   - 已通过 Python 单元测试：
     - `tests.test_service_server_platform_routes`
     - `tests.test_rpa_bridge_platform_routing`
     - `tests.test_service_truth_store`
     - `tests.test_cache_snapshot`
   - 已在 `build/Desktop_Qt_6_10_2_MSVC2022_64bit-Debug` 下重新编译并通过：
     - `yy_ai_customer_service`

### 2026-06-03（P1：sidecar 事件能力与 IPC 命名壳第一步）

开始进入 P1，先从 C++ IPC 层把主调用语义从 `Rpa*` 包装迁到 `Platform*` / platform event 语义，降低后续维护中的旧命名误导：

1. **IPC 类型与方法命名**
   - `Ipc::PlatformCommandRequest` / `PlatformCommandResponse` 成为主类型
   - 旧 `RpaCommandRequest` / `RpaCommandResponse` 保留为兼容别名
   - `IpcService::sendPlatformCommandViaWebSocket()` 成为主发送入口
   - `IpcService::fetchPlatformReplay()` / `dispatchPlatformReplayEvents()` 成为 replay 主入口
   - 旧 `sendRpaCommandViaWebSocket()`、`fetchRpaReplay()`、`dispatchRpaReplayEvents()` 保留为包装

2. **事件信号命名**
   - 新增 `platformEventReceived`
   - 新增 `platformEventBridgeStateChanged`
   - WebSocket 实时事件和 replay 事件会同时发出 platform 主信号与旧 RPA 兼容信号
   - `WechatRPAAdapter` / `QianniuRPAAdapter` 已切到 platform 主信号和 platform 命名方法

3. **验证**
   - `ReadLints` 检查相关 C++ 文件：无新增诊断
   - 已在 `build/Desktop_Qt_6_10_2_MSVC2022_64bit-Debug` 下重新编译并通过：
     - `yy_ai_customer_service`
     - `yy_ai_customer_service_data_tests`
   - 已通过 CTest：
     - `yy_ai_customer_service_data_tests`
   - `yy_ai_customer_service_message_router_tests` 不是当前构建目录中的有效 target，本轮未单独构建该目标

### 2026-06-03（P0：C++ 消费 replay 与千牛持续 observer）

继续把 C++ 本地库往缓存恢复层退，让 C++ 启动/重连后优先消费 Python 服务端 replay/backfill，而不是重新承担历史补扫职责：

1. **C++ replay 消费**
   - `IpcService` 新增 `fetchRpaReplay()`
   - `IpcService` 新增 `dispatchRpaReplayEvents()`，把 replay 事件重新走现有 `rpaEventReceived` -> 平台 adapter -> `MessageRouter` 路径
   - `ConversationDao` 新增 `rpaReplayCursor()` / `setRpaReplayCursor()`
   - `AggregateChatForm` 在 Python 服务可用后按 `wechat` / `qianniu` 先拉 `/api/rpa/replay`，保存 `rpa_replay_cursor/*`，再拉 snapshot/backfill

2. **千牛持续 observer**
   - `QianniuSidecarAdapter` connect 后启动后台 observer loop
   - observer 复用既有 `scan_unread_and_fetch`，持续扫描未读会话并抓取消息
   - disconnect 时停止 observer
   - 微信和千牛 sidecar 均已具备持续 observer 路径

3. **验证**
   - `ReadLints` 检查相关 Python/C++ 文件：无新增诊断
   - 已通过 Python 单元测试：
     - `tests.test_qianniu_sidecar_adapter`
     - `tests.test_service_truth_store`
     - `tests.test_rpa_bridge_platform_routing`
     - `tests.test_cache_snapshot`
   - 已在 `build/Desktop_Qt_6_10_2_MSVC2022_64bit-Debug` 下重新编译并通过：
     - `yy_ai_customer_service`
     - `yy_ai_customer_service_data_tests`
   - 已通过 CTest：
     - `yy_ai_customer_service_data_tests`

### 2026-06-03（P0：服务端事件 replay 与历史重建骨架）

继续补齐 Python 服务端的 replay/backfill 能力，让 C++ 断线或重启后不再只能依赖本地库历史兜底：

1. **持久事件日志**
   - `PythonServiceTruthStore` 新增 `rpa_events` 表
   - `conversation_observed`、`message_observed`、`message_sent`、`send_failed` 会写入持久事件日志
   - 事件日志按 `event_id` 去重，避免重复观测造成 replay 重复事件

2. **事件 replay API**
   - `PythonServiceTruthStore.replay_events()` 支持按 `platform`、`cursor`、`limit` 查询持久事件
   - `RpaBridge` 新增 `replay()`
   - `GET /api/rpa/replay` 暴露服务端事件重放能力
   - `/api/rpa/events` 仍保留内存实时队列语义；历史重建继续由 `/api/cache/snapshot` 承担

3. **验证**
   - `ReadLints` 检查相关 Python 文件：无新增诊断
   - 已通过 Python 单元测试：
     - `tests.test_service_truth_store`
     - `tests.test_rpa_bridge_platform_routing`
     - `tests.test_cache_snapshot`

### 2026-06-03（P0：Python 服务端接管平台事件与发送状态写库）

继续收口数据真相源，让 Python 服务端先负责记录真实平台事件与基础发送状态，C++ 客户端继续只消费事件并重建本地缓存：

1. **服务端事实库写入层**
   - 新增 `python/service/truth_store.py`
   - `PythonServiceTruthStore` 负责把平台事件写入 Python 服务端事实库
   - `conversation_observed` 按 `platform + conversation_key` upsert `conversations`
   - `message_observed` 按 `platform_msg_id` / `client_message_id` 幂等 upsert `messages`
   - 写入统一标记 `cache_origin = python_service_truth`

2. **桥接入口接管发送状态**
   - `RpaEventStore` 接入 `PythonServiceTruthStore`
   - 平台事件进入内存队列和推送给 C++ 前，先尝试写入服务端事实库
   - `send_message` 到达 Python 服务端时先记录 pending 出站消息
   - `message_sent` / `send_failed` 按 `client_message_id` 更新 sent/failed
   - 写库失败会记录异常并保留事件转发，避免单次 DB 问题阻断平台采集

3. **验证**
   - `ReadLints` 检查相关 Python 文件：无新增诊断
   - 已通过 Python 单元测试：
     - `tests.test_service_truth_store`
     - `tests.test_rpa_bridge_platform_routing`
     - `tests.test_cache_snapshot`

### 2026-06-03（P0：snapshot 事实源脱离 C++ 本地缓存）

继续削弱 C++ 本地持久化的主真相源职责，先把 `/api/cache/snapshot` 的数据来源从“可能命中 Qt AppData 的客户端本地库”收口为明确的 Python 服务端事实库：

1. **服务端 snapshot DB 解析收口**
   - `python/service/cache_snapshot.py` 新增 `resolved_snapshot_db_path()`
   - snapshot 默认读取仓库 `database/service.db`
   - 可通过 `AI_CUSTOMER_SERVICE_SERVER_DB` 或 `AI_CUSTOMER_SERVICE_SNAPSHOT_DB` 指定服务端事实库
   - 不再复用 `rpa.db.connection.resolved_default_db_path()`，避免自动命中 C++ AppData 本地缓存库
   - snapshot 返回 `source_role = python_service_db`

2. **客户端可观测性**
   - `IpcService::fetchCacheSnapshot()` 日志增加 `sourceRole`
   - `python/service/README.md` 记录 snapshot 的服务端 DB 环境变量

3. **验证**
   - `ReadLints` 检查相关 Python/C++ 文件：无新增诊断
   - 已通过 Python 单元测试：
     - `tests.test_cache_snapshot`
   - 已在 `build/Desktop_Qt_6_10_2_MSVC2022_64bit-Debug` 下重新编译并通过：
     - `yy_ai_customer_service`

### 2026-06-03（P0：snapshot 清理缺失项与游标化回灌）

继续推进服务端 snapshot 接管 UI 恢复/缓存重建。本轮在上一轮非破坏性 upsert 基础上，增加按平台 cursor 的增量回灌，以及全量平台快照范围内的缺失项清理：

1. **Python 快照游标**
   - `GET /api/cache/snapshot` 支持 `cursor`
   - 返回 `cursor`、`snapshot_cursor`、`full_snapshot`
   - 无 cursor 时按平台做全量快照；有 cursor 时只返回更新时间晚于 cursor 的会话快照

2. **C++ 游标与缺失项清理**
   - `IpcService::fetchCacheSnapshot()` 支持传入 cursor
   - `ConversationDao` 新增 `snapshotCursor()` / `setSnapshotCursor()`
   - `ConversationDao` 新增 `deleteMissingSnapshotCacheConversations()`
   - `MessageDao` 新增 `deleteMissingSnapshotCacheMessages()`
   - `AggregateChatForm` 按 `wechat` / `qianniu` 分平台拉取 snapshot，写回后保存平台 cursor
   - 全量平台快照会清理上一轮 `server_snapshot_cache` 中缺失的会话和消息；pending、草稿、游标和非 snapshot 来源缓存不删除

3. **验证**
   - `ReadLints` 检查相关 Python/C++ 文件与测试：无新增诊断
   - 已通过 Python 单元测试：
     - `tests.test_cache_snapshot`
   - 已在 `build/Desktop_Qt_6_10_2_MSVC2022_64bit-Debug` 下重新编译并通过：
     - `yy_ai_customer_service`
     - `yy_ai_customer_service_data_tests`
   - 已通过 CTest：
     - `yy_ai_customer_service_data_tests`

### 2026-06-03（P0：服务端 snapshot 写回本地缓存第一步）

在 `/api/cache/snapshot` 和 `IpcService::fetchCacheSnapshot()` 已具备后，继续推进 C++ 侧解析并写回本地缓存。本轮采用非破坏性 upsert，先让服务端快照真正参与 UI 恢复/缓存重建，不清空本地表：

1. **新增 snapshot 写回 DAO 入口**
   - `ConversationDao` 新增 `upsertSnapshotCacheConversation()`
   - `MessageDao` 新增 `upsertSnapshotCacheMessage()`
   - 会话按 `platform + platform_conversation_id` upsert
   - 消息按 `platform_msg_id` 或 `client_message_id` 幂等 upsert
   - 写回数据统一标记 `cache_origin = server_snapshot_cache`

2. **接入 UI 恢复流程**
   - `AggregateChatForm` 新增 `applyCacheSnapshotToLocalCache()`
   - Python sidecar 可用后拉取 snapshot，先写回本地缓存，再执行 `reloadFromLocalCache()`
   - 当前不删除本地缺失项，不清空 pending/草稿/游标，避免一次性扩大风险

3. **验证**
   - `ReadLints` 检查相关 C++ 文件与测试：无新增诊断
   - `tests/test_data_access.cpp` 补充 snapshot 会话/消息写回和重复 upsert 断言
   - 已在 `build/Desktop_Qt_6_10_2_MSVC2022_64bit-Debug` 下重新编译并通过：
     - `yy_ai_customer_service`
     - `yy_ai_customer_service_data_tests`
   - 已通过 CTest：
     - `yy_ai_customer_service_data_tests`

### 2026-06-03（P0：服务端快照回灌接口第一步）

开始从“只改 C++ 本地缓存语义”进入“Python 服务端提供回灌/重建接口，C++ 触发拉取并重建本地缓存”的阶段。本轮先落最小只读快照和 C++ 拉取探测，暂不写回本地缓存：

1. **Python 服务端只读快照接口**
   - 新增 `python/service/cache_snapshot.py`
   - 新增 `GET /api/cache/snapshot`
   - 支持 `platform`、`conversation_limit`、`message_limit`
   - 返回会话及其消息快照、`conversation_count`、`message_count` 和 `db_path`

2. **C++ 触发拉取骨架**
   - `IpcService` 新增 `fetchCacheSnapshot()`
   - `AggregateChatForm` 在 Python sidecar 服务可用后触发一次快照拉取探测
   - 当前只记录快照数量，不执行本地缓存重建，避免一次性扩大改动面

3. **验证**
   - `ReadLints` 检查相关 Python/C++ 文件：无新增诊断
   - 已通过 Python 单元测试：
     - `tests.test_cache_snapshot`
   - 已在 `build/Desktop_Qt_6_10_2_MSVC2022_64bit-Debug` 下重新编译并通过：
     - `yy_ai_customer_service`

### 2026-06-03（P0：UI 恢复入口命名收口）

继续按 `C++客户端旧链路盘点与解耦差距.md` 的 P0 小步推进，把 UI 恢复入口从 `database` 主路径命名收口为 `local cache`：

1. **新增本地缓存恢复主入口**
   - `ConversationManager` 新增 `reloadFromLocalCache()`
   - `AggregateChatForm` 新增 `reloadFromLocalCache()`
   - 旧 `reloadFromDatabase()` 保留为兼容包装，避免一次性扩大改动面

2. **切换调用**
   - Python sidecar 服务可用后的恢复调用改走 `reloadFromLocalCache()`
   - 运行日志已保持 `local cache` 语义

3. **验证**
   - `ReadLints` 检查相关 C++ 文件：无新增诊断
   - 已在 `build/Desktop_Qt_6_10_2_MSVC2022_64bit-Debug` 下重新编译并通过：
     - `yy_ai_customer_service`

### 2026-06-03（P0：本地历史读取缓存语义收口）

继续按 `C++客户端旧链路盘点与解耦差距.md` 的 P0 小步推进，把会话列表分栏、自动回复候选判断和 AI 最近入站快照这些“读本地消息历史”的入口改为显式的本地历史缓存读取：

1. **新增历史缓存读取入口**
   - `MessageDao` 新增 `lastCachedMessageForConversation()`
   - `MessageDao` 新增 `lastCachedDirectionsByConversation()`
   - `MessageDao` 新增 `latestCachedInboundSnapshot()` / `latestCachedInboundContent()`
   - 旧历史读取方法保留，避免一次性扩大改动面

2. **切换上层调用**
   - `ConversationAppService::isAggregateAutoReplyCandidate()` 改走最后一条缓存消息
   - `AggregateChatForm::refreshConversationList()` 改走本地缓存最后消息方向
   - `AiChatAppService::buildAggregateReplyRequest()` 改走本地缓存最近入站快照
   - `tests/test_data_access.cpp` 补充本地历史缓存读取断言

3. **验证**
   - `ReadLints` 检查相关 C++ 文件与测试：无新增诊断
   - 已在 `build/Desktop_Qt_6_10_2_MSVC2022_64bit-Debug` 下重新编译并通过：
     - `yy_ai_customer_service`
     - `yy_ai_customer_service_data_tests`
   - 已通过 CTest：
     - `yy_ai_customer_service_data_tests`

### 2026-06-03（P0：本地发送状态缓存语义收口）

继续按 `C++客户端旧链路盘点与解耦差距.md` 的 P0 小步推进，把客户端发送链路中的 pending / sent / failed 状态从“普通本地消息状态”改为显式的出站缓存状态：

1. **新增出站缓存入口**
   - `MessageDao` 新增 `createOutboundCacheMessage()`
   - `MessageDao` 新增 `latestPendingOutboundCache()` / `latestPendingOutboundCacheByClientMessageId()`
   - `MessageDao` 新增 `updateOutboundCacheDeliveryState()`
   - 旧 pending 查找和投递状态更新方法保留，避免一次性扩大改动面

2. **切换发送主路径**
   - `MessageRouter::sendMessage()` 创建本地 pending 消息改走出站缓存入口
   - sidecar 发送成功回执匹配 pending 消息改走出站缓存入口
   - sidecar 发送失败回执更新本地 failed 状态改走出站缓存入口
   - `tests/test_data_access.cpp` 补充出站缓存创建、查找和状态更新断言

3. **验证**
   - `ReadLints` 检查相关 C++ 文件与测试：无新增诊断
   - 已在 `build/Desktop_Qt_6_10_2_MSVC2022_64bit-Debug` 下重新编译并通过：
     - `yy_ai_customer_service`
     - `yy_ai_customer_service_data_tests`
   - 已通过 CTest：
     - `yy_ai_customer_service_data_tests`

### 2026-06-03（P0：UI 状态缓存语义收口）

继续按 `C++客户端旧链路盘点与解耦差距.md` 的 P0 小步推进，把草稿和最近会话恢复从“普通本地库状态”改为显式的客户端 UI 状态缓存入口：

1. **新增 UI 状态缓存入口**
   - `ConversationDao` 新增 `cachedDraftForConversation()`
   - `ConversationDao` 新增 `saveCachedDraft()` / `clearCachedDraft()`
   - `ConversationDao` 新增 `lastSelectedCachedConversationId()` / `setLastSelectedCachedConversationId()`
   - 旧草稿和最近会话方法保留，避免一次性扩大改动面

2. **切换上层调用**
   - `AggregateChatForm` 的草稿保存、恢复、清理改走缓存语义入口
   - `AggregateChatForm` 的最近会话恢复改走缓存游标入口
   - `ConversationManager` 选择/关闭/删除会话时保存最近会话游标改走缓存语义入口
   - `ConversationManager` 恢复日志继续从 `database` 语义收口为 `local cache`

3. **验证**
   - `ReadLints` 检查相关 C++ 文件与测试：无新增诊断
   - 已在 `build/Desktop_Qt_6_10_2_MSVC2022_64bit-Debug` 下重新编译并通过：
     - `yy_ai_customer_service_data_tests`
   - 已通过 CTest：
     - `yy_ai_customer_service_data_tests`

### 2026-06-03（P0：客户端缓存读取边界收口）

按 `C++客户端旧链路盘点与解耦差距.md` 的 P0 优先级，继续把 `MessageRouter + DAO` 周边从“客户端本地库是主真相源”收口为“客户端本地缓存/展示层”：

1. **新增缓存读取入口**
   - `ConversationDao` 新增 `listCachedConversations()`
   - `MessageDao` 新增 `listCachedMessages()`
   - 旧 `listAll()` / `listByConversation()` 保留，避免一次性扩大改动面

2. **上层读取语义切换**
   - `ConversationManager::allConversations()` 改为读取本地会话缓存
   - `ConversationManager::messages()` 改为读取本地消息缓存
   - `ConversationAppService` 的聚合 UI 数据入口同步切到缓存读取方法
   - 聚合 UI / ConversationManager 的恢复日志从 `database` 改为 `local cache`

3. **验证**
   - `ReadLints` 检查相关 C++ 文件与测试：无新增诊断
   - 已在 `build/Desktop_Qt_6_10_2_MSVC2022_64bit-Debug` 下重新编译并通过：
     - `yy_ai_customer_service`
     - `yy_ai_customer_service_data_tests`
   - 已通过 CTest：
     - `yy_ai_customer_service_data_tests`

### 2026-06-03（P0：C++ 旧链路盘点文档再校准）

在旧平台别名、客户端补偿扫描和千牛旧 DB 桥陆续收口后，重新校准 `C++客户端旧链路盘点与解耦差距.md`，避免后续继续按过时基线推进：

1. **更新当前主路径**
   - 明确默认平台注册已包含 `SimPlatformAdapter`、`QianniuRPAAdapter`、`WechatRPAAdapter`
   - 明确千牛已从旧共享 SQLite inbox 桥切到 `IpcService + QianniuRPAAdapter + python/rpa/platforms/qianniu`
   - 明确平台主命名已统一为 `wechat` / `qianniu`

2. **更新已完成收口项**
   - 千牛旧 DB 桥已退出主路径
   - 微信/千牛客户端主动补偿扫描已删除
   - 旧平台别名和旧平台协议字段已完全退出
   - `MessageRouter + DAO` 已开始用 `cache_scope/cache_origin` 和 observed-cache DAO 入口标记客户端本地库缓存语义

3. **重排后续优先级**
   - P0 重点从“删旧桥/删补偿扫描”转为“继续把 `MessageRouter + DAO` 从主真相源收口为缓存/展示层”
   - P1 重点转为 sidecar 事件能力、health、replay/backfill 以及 `Rpa*` 命名壳收口
   - P2 保持 README、工程骨架文档、UI 帮助文案和资源命名清理

验证：

- `ReadLints` 检查盘点文档：无新增诊断
- 已复查关键字，未发现“千牛未接入”“补偿扫描并存”“旧别名兼容”等过时结论残留

### 2026-06-03（P0：旧平台别名完全退出）

在平台主命名统一为 `wechat` / `qianniu` 后，继续把旧别名兼容和旧示例彻底清掉：

1. **删除旧平台别名输入兼容**
   - 更新 `src/models/unifiedmodels.cpp`
   - 不再把旧平台别名解析为有效平台
   - 更新 `src/services/platforms/wechatrp_adapter.cpp`
   - 更新 `src/services/platforms/qianniurp_adapter.cpp`
   - sidecar 事件只接受标准平台名

2. **删除 Python 旧字段兼容**
   - 更新 `python/service/rpa_bridge.py`
   - 更新 `python/service/ai_suggestion.py`
   - 更新 `python/rpa/platforms/wechat/adapter.py`
   - 更新 `python/rpa/platforms/qianniu/adapter.py`
   - 请求入口只读取 `platform` 和 `command`

3. **清理旧示例和旧说明**
   - 更新 `docs/docs-new/design/*` 与 `docs/docs-old/*` 中残留的旧平台值示例
   - 更新 `tests/test_rpa_bridge_platform_routing.py`，测试只使用标准平台名

当前判断：

- 全仓已无旧平台别名文本残留
- 全仓已无旧 `platform` / `command` 兼容字段残留
- 平台协议现在只认 `wechat` / `qianniu` 和 `platform` / `command`

验证：

- `ReadLints` 检查最近修改文件：无新增诊断
- 已在 `build/Desktop_Qt_6_10_2_MSVC2022_64bit-Debug` 下重新编译并通过：
  - `yy_ai_customer_service`
  - `yy_ai_customer_service_data_tests`
  - `yy_ai_customer_service_router_tests`
- 已通过 Python 定向测试：
  - `tests.test_rpa_bridge_platform_routing`
  - `tests.test_wechat_sidecar_adapter`
  - `tests.test_qianniu_sidecar_adapter`

### 2026-06-03（P0：平台命名统一为 wechat / qianniu）

继续收口客户端与 Python sidecar 之间的协议边界，把平台命名和 `IpcService` 里的旧兼容字段一起统一到新规范：

1. **客户端平台命名统一**
   - 更新 `src/models/unifiedmodels.cpp`
   - `Models::PlatformType` 对外字符串统一为 `wechat` / `qianniu`
   - 旧平台别名已完全退出主链路
   - 更新 `WechatRPAAdapter`、`QianniuRPAAdapter`、`MessageRouter`、`ConversationDao`、`MessageDao`、聚合 UI 过滤等相关字符串判断

2. **`IpcService` / `IpcTypes` 收口旧协议字段**
   - 更新 `src/ipc/ipctypes.h`
   - `AiSuggestionRequest`、`RpaCommandRequest` 内部字段从 `platformType` 收口为 `platform`
   - 更新 `src/ipc/ipcservice.cpp`
   - C++ 发给 Python 的 payload 现在只使用 `platform`

3. **Python bridge 和 sidecar 平台常量同步**
   - 更新 `python/service/rpa_bridge.py`
   - 更新 `python/service/server.py`
   - 更新 `python/service/ai_suggestion.py`
   - 更新 `python/rpa/platforms/wechat/adapter.py`
   - 更新 `python/rpa/platforms/qianniu/adapter.py`
   - 新主路径统一为 `wechat` / `qianniu`
   - 不再保留旧平台别名输入兼容映射

4. **测试同步**
   - 更新 C++ 测试：`tests/test_data_access.cpp`、`tests/test_message_router.cpp`
   - 更新 Python 测试：`tests/test_rpa_bridge_platform_routing.py`、`tests/test_wechat_sidecar_adapter.py`、`tests/test_qianniu_sidecar_adapter.py`
   - 并补齐微信测试桩对 `tail_only` 参数的新签名兼容

当前判断：

- 当前主链路的标准平台名已经统一为 `wechat` / `qianniu`
- `IpcService` 不再继续扩散旧平台字段，协议表面语义更接近“客户端 <-> Python 服务端”
- 旧平台别名已在后续收口中完全退出

验证：

- `ReadLints` 检查最近修改文件：无新增诊断
- 已在 `build/Desktop_Qt_6_10_2_MSVC2022_64bit-Debug` 下重新编译并通过：
  - `yy_ai_customer_service`
  - `yy_ai_customer_service_data_tests`
  - `yy_ai_customer_service_router_tests`
- 已通过 Python 定向测试：
  - `tests.test_rpa_bridge_platform_routing`
  - `tests.test_wechat_sidecar_adapter`
  - `tests.test_qianniu_sidecar_adapter`

### 2026-06-03（P0：删除客户端主动补偿扫描链路）

继续沿着“客户端本地库降级为缓存层”的方向收口，把 `WechatRPAAdapter / QianniuRPAAdapter` 里剩余的客户端主动补偿扫描链路删掉，让消息主路径彻底回到 `Python sidecar -> 事件桥 -> C++ 展示/缓存`：

1. **删除微信客户端侧定时补偿扫描**
   - 更新 `src/services/platforms/wechatrp_adapter.h/cpp`
   - 删除 `QTimer + pollOnce() + scan_unread_and_fetch`
   - `startListening()` 只保留 sidecar `connect` 和事件桥接入

2. **删除千牛客户端侧定时补偿扫描**
   - 更新 `src/services/platforms/qianniurp_adapter.h/cpp`
   - 删除 `QTimer + pollOnce() + scan_unread_and_fetch`
   - 保留 `connect`、事件接收和 `send_message`

3. **同步盘点文档状态**
   - 更新 `docs/docs-new/design/C++客户端旧链路盘点与解耦差距.md`
   - 明确“客户端主动补偿扫描”已经退出主路径

当前判断：

- 当前 C++ 客户端已经不再主动驱动 `scan_unread_and_fetch`
- 客户端侧平台适配器进一步收口成“命令入口 + 事件接收 + 兼容映射层”
- 如果后续出现会话/消息漏回灌，优先应该补 Python sidecar 的 observer / health / replay 能力，而不是把轮询扫描重新放回客户端

验证：

- `ReadLints` 检查最近修改文件：无新增诊断
- 已在 `build/Desktop_Qt_6_10_2_MSVC2022_64bit-Debug` 下重新编译并通过 `yy_ai_customer_service`

### 2026-06-03（P0：客户端本地库降级为缓存层第一步）

围绕 `MessageRouter / ConversationDao / MessageDao / Database`，先把客户端本地 SQLite 从“默认主持久化语义”往“显式缓存层语义”收一小步，不一次性推翻当前恢复链路，而是先收口写入边界和表字段语义：

1. **给会话/消息记录补显式缓存语义字段**
   - 更新 `src/data/database.cpp`
   - `conversations`、`messages` 新增 `cache_scope`、`cache_origin`
   - 旧库升级路径也补齐相应 `ALTER TABLE`
   - 同时把遗留 `rpa_inbox_messages` 的扩展列迁移补齐，避免 legacy 升级测试继续漂移

2. **DAO 增加“平台观测数据写入本地缓存”入口**
   - 更新 `src/data/conversationdao.h/cpp`
   - 新增 `ConversationDao::upsertObservedCacheConversation()`
   - 更新 `src/data/messagedao.h/cpp`
   - 新增 `MessageDao::createObservedCacheMessage()`
   - 会话/消息落库时统一写入 `local_cache` / `platform_observed_cache` 或 `manual_outbound_cache` 等来源标记

3. **`MessageRouter` 不再把平台观测数据当普通持久化写入**
   - 更新 `src/core/messagerouter.cpp`
   - `conversation_observed` 与 `message_observed` 统一走 DAO 的缓存写入接口
   - `ensureConversation()` 创建平台会话时也改走 observed-cache 语义
   - 手工发送的待发送消息继续保留现有 UI/恢复链路，但也显式标成 `manual_outbound_cache`

4. **测试补齐缓存语义验证**
   - 更新 `tests/test_message_router.cpp`
   - 更新 `tests/test_data_access.cpp`
   - 断言会话/消息记录带有缓存字段，并修正旧测试里对初始会话状态的过时预期

当前判断：

- 这一轮还没有把客户端库彻底降成“可随时重建”的纯缓存，但已经先把“平台观测数据如何写入本地库”统一收口成缓存语义
- 后续如果继续推进 P0/P1，就可以沿着 `ObservedCache` 入口继续把 UI 恢复、补偿扫描、服务端真相回灌拆开，而不是再从 `MessageRouter` 里四处找散落直写

验证：

- `ReadLints` 检查最近修改文件：无新增诊断
- 已在 `build/Desktop_Qt_6_10_2_MSVC2022_64bit-Debug` 下重新编译并通过：
  - `yy_ai_customer_service_data_tests`
  - `yy_ai_customer_service_router_tests`

### 2026-06-03（P0：千牛旧 DB 桥切换到 sidecar）

继续按 `C++客户端旧链路盘点与解耦差距.md` 的 P0 收口客户端侧旧链路，先处理最典型的旧 SQLite inbox 桥：

1. **千牛 C++ 适配器从共享 SQLite inbox 改为 sidecar 命令/事件桥**
   - 更新 `src/services/platforms/qianniurp_adapter.h/cpp`
   - 不再轮询 `rpa_inbox_messages`
   - 改为通过 `IpcService` 向 `qianniu` sidecar 发 `connect`、`scan_unread_and_fetch`、`send_message`
   - 通过事件桥接收 `conversation_observed`、`message_observed`、`message_sent`、`account_health_changed`
   - 为兼容现有 UI 和平台筛选，C++ 内部平台名暂时仍保留 `qianniu`

2. **默认平台启动流程接入千牛 sidecar 适配器**
   - 更新 `src/core/platformbootstrap.cpp`
   - 默认平台注册从 `SimPlatformAdapter + WechatRPAAdapter` 扩展为 `SimPlatformAdapter + QianniuRPAAdapter + WechatRPAAdapter`

3. **收口一处路由层直写 SQL**
   - 更新 `src/data/conversationdao.h/cpp`
   - 新增 `ConversationDao::updateDisplayName()`
   - `src/core/messagerouter.cpp` 不再直接拼 SQL 更新会话名，先收回 DAO 边界

4. **弱化数据库层旧共享库语义**
   - 更新 `src/data/database.cpp`
   - 默认日志说明改为“客户端本地缓存库”
   - 不再强调 Python RPA 默认与该路径对齐

当前判断：

- 千牛最典型的“Python 写共享 SQLite inbox，C++ 轮询读表”旧链路已经退出主路径
- 当前 P0 剩余的更大问题仍是：`MessageRouter + DAO` 继续承担较强客户端持久化职责，尚未完全对齐“Python 服务端数据库是唯一真相源”
- 千牛 sidecar 当前仍偏命令式，C++ 侧保留低频补偿扫描，后续若 Python 千牛具备持续 observer，可继续收口

验证：

- `ReadLints` 检查最近修改的 C++ 文件：无诊断错误
- 未执行完整 Qt/CMake 命令行构建，建议在 Qt Creator 中继续验证千牛 sidecar 联调

### 2026-06-03（C++ 旧链路盘点基线）

本轮先不改 C++ 实现，先把当前客户端侧仍残留的旧链路和与目标解耦架构的差距收口成基线文档，避免后续接千牛 sidecar 或继续改 `MessageRouter` 时边界再次漂移。

新增文档：

- `docs/docs-new/design/C++客户端旧链路盘点与解耦差距.md`

本轮盘点结论：

- 旧 `src/services/rpa/`、旧 `src/services/wechat/`、旧 RPA 管理/控制台/微信工作台 UI 已退出当前工作树，不再是主路径。
- 当前客户端默认主路径已收敛为：`main.cpp` -> `PlatformBootstrap` -> `WechatRPAAdapter` / `SimPlatformAdapter` + `IpcService` + `PythonServiceConnectionDialog`。
- `src/services/platforms/qianniurp_adapter.*` 仍保留“共享 SQLite inbox 队列”旧链路，但未注册进默认平台启动流程，是当前最明确的旧桥残留。
- `src/services/platforms/wechatrp_adapter.*` 虽然主路径已是 sidecar 命令/事件桥，但仍保留低频 `scan_unread_and_fetch` 补偿扫描，说明客户端主动驱动自动化的过渡层仍在。
- `src/core/messagerouter.*` 与 `src/data/*` 仍承担较强本地持久化职责，和“Python 服务端数据库是唯一真相源，C++ 只保留缓存”的目标尚未完全对齐。
- `README.md`、`工程骨架与模块边界.md`、`robotassistantwidget.cpp` 等仍残留旧目录、旧工作台、旧 RPA 管理语义，已列入后续清理范围。

建议优先级：

- P0：`qianniurp_adapter.*`、`messagerouter.*`、`database.cpp`
- P1：`wechatrp_adapter.*`、`ipcservice.*`、`ipctypes.*`
- P2：README、设计文档、UI 文案和资源命名

### 2026-05-29（微信 RPA sidecar 边界收口）

**将微信 RPA 具体实现从 service 层迁入平台适配目录**

在第一阶段 RPA HTTP 接口已经打通后，继续收口 Python sidecar 边界：

1. **新增微信平台适配器**
   - 新增 `python/rpa/platforms/wechat/adapter.py`
   - 承接 `connect`、`health_check`、可见会话采集、可见消息采集、草稿回填和受控发送命令
   - 保持现有事件结构、命令名称和人工确认 token 不变

2. **瘦身 service bridge**
   - `python/service/rpa_bridge.py` 只保留内存事件队列、HTTP bridge 单例和平台分发
   - `python/service/server.py` 的 `/api/rpa/*` 接口未变

3. **验证**
   - 已通过 Python AST 语法检查
   - 已在 `python/` 工作目录下验证 `service.rpa_bridge` 可导入并处理基础事件/错误平台分支

当前判断：
- 微信 RPA 已从临时 service 实现过渡到平台适配器边界
- 下一步应继续按 `D:/wechat` 的 `detector / reader / sender / stability` 分层拆解自动化内核
- Qt/C++ 侧无需因本次迁移调整调用路径

---

### 2026-05-29（微信 RPA 自动化内核分层骨架）

**对齐 `D:/wechat` 的 detector / reader / sender / stability 职责**

在微信平台适配器迁入 `python/rpa/platforms/wechat/adapter.py` 后，继续建立自动化内核分层：

1. **新增分层模块**
   - `python/rpa/platforms/wechat/detector.py`：微信窗口探测、健康探测、可见会话采集
   - `python/rpa/platforms/wechat/reader_v2.py`：可见消息读取入口
   - `python/rpa/platforms/wechat/sender_v2.py`：草稿回填和受控发送入口
   - `python/rpa/platforms/wechat/stability.py`：自动化互斥锁和失败计数

2. **适配器改为依赖分层模块**
   - `WechatSidecarAdapter` 不再直接调用 UIA reader/sender 细节
   - UIA、配置读取和窗口操作仍保持方法内懒加载，避免 Python sidecar 启动时加载重依赖
   - HTTP API、事件结构、命令名保持不变

3. **验证**
   - 已通过 Python AST 语法检查
   - 已在 `python/` 工作目录下验证 `service.rpa_bridge` 与新微信模块可导入

当前判断：
- 新链路已经具备可替换的微信自动化内核边界
- 后续可以逐步把 `D:/wechat` 中更强的候选控件评分、角色判断、失败冷却和调试证据能力迁入这些模块

---

### 2026-05-29（微信 RPA UIA 候选控件评分兜底）

**迁入 `D:/wechat` 路线中的候选控件打分思想**

在 `detector / reader_v2 / sender_v2` 分层骨架基础上，补齐第一批稳定性增强：

1. **新增 UIA 候选评分模块**
   - 新增 `python/rpa/platforms/wechat/uia_scoring.py`
   - 提供安全属性读取、BFS 控件遍历、矩形读取和候选控件打分
   - 覆盖会话列表、消息列表、输入框、发送按钮四类核心控件

2. **接入兜底路径**
   - `detector.py`：固定 `session_list` 识别失败时，使用候选评分定位会话列表并采集会话项
   - `reader_v2.py`：固定 `chat_message_list` 读取失败时，使用候选评分定位消息列表并提取消息文本
   - `sender_v2.py`：固定输入框或发送按钮查找失败时，使用候选评分兜底

3. **测试与验证**
   - 新增 `tests/test_wechat_uia_scoring.py`
   - 覆盖四类候选控件评分
   - 已通过 `python tests/test_wechat_uia_scoring.py`
   - 已通过相关 Python 文件 AST 语法检查
   - 已验证 Python sidecar bridge 和 `uia_scoring` 可导入

当前判断：
- 微信 RPA 已具备比固定 AutomationId 更稳的控件识别兜底
- 下一步优先补消息方向/角色判断，并把 `D:/wechat` 中的失败冷却和调试截图证据按当前项目边界迁入

---

### 2026-05-29（微信 RPA 消息角色判断与发送按钮策略）

**迁入 `D:/wechat` 的角色判断和多策略发送思路**

在 UIA 候选控件评分兜底基础上，继续补齐微信 RPA 自动化内核：

1. **消息角色判断**
   - 新增 `python/rpa/platforms/wechat/role_judgement.py`
   - 优先尝试气泡截图左右方差判断，失败时退回消息列表中心线位置判断
   - `reader_v2.py` 会给消息 sample 附加 `direction`、`sender_role`、`direction_method`、`role_confidence`、左右方差等字段
   - `adapter.py` 生成 `message_observed` 时优先使用上述字段，并把判断方法和置信度写入 metadata

2. **发送按钮多策略点击**
   - 新增 `python/rpa/platforms/wechat/click_strategy.py`
   - `sender_v2.py` 发送按钮点击顺序扩展为：后台窗口消息 SendMessage、后台窗口消息 PostMessage、InvokePattern、UIA click、前台鼠标点击
   - `send_result_observed` metadata 会记录实际命中的 `method` 和是否使用 `foreground`

3. **截图辅助**
   - 新增 `python/rpa/platforms/wechat/screenshot.py`
   - 当前用于角色判断中的气泡截图裁剪，失败时静默退回位置判断

4. **测试与验证**
   - 新增 `tests/test_wechat_role_and_send.py`
   - 已通过 `python tests/test_wechat_role_and_send.py`
   - 已通过 `python tests/test_wechat_uia_scoring.py`
   - 已通过相关 Python 文件 AST 语法检查
   - 已验证 Python sidecar bridge 和新角色判断类型可导入
   - 未执行真实微信点击测试，避免在开发机上触发桌面发送动作

当前判断：
- 微信消息方向不再只依赖固定 `center > 900` 这种绝对坐标启发
- 发送按钮不再只有单一 UIA click 路径，后续可根据真实微信版本观察命中率调整策略顺序
- 下一步应补失败冷却、调试证据保存，并给 C++ `message_observed -> PlatformMessage` 映射增加单元测试

---

### 2026-05-29（微信 RPA health 联调异常修复）

**修复 `/api/rpa/health` 连接被意外关闭**

联调时复现 PowerShell `Invoke-RestMethod` 报错“基础连接已经关闭：连接被意外关闭”。排查结果：

- `/api/health` 正常，说明 sidecar 端口和 HTTP 服务可用
- `/api/rpa/health` 触发 `uiautomation/comtypes`，但 `ThreadingHTTPServer` 的请求处理线程未调用 COM 初始化
- Python handler 抛出 `[WinError -2147221008] 尚未调用 CoInitialize`，导致连接被服务端直接关闭

修复：

- `python/rpa/platforms/wechat/stability.py` 新增 `uia_guard`
- `uia_guard` 在请求线程中调用 `comtypes.CoInitialize()` / `CoUninitialize()`
- `WechatSidecarAdapter.command()` 和 `health()` 改为通过 `uia_guard` 执行 UIA 调用

验证：

- `GET /api/health` 正常返回
- `GET /api/rpa/health?platform=wechat` 不再断开连接，能返回结构化 JSON
- 当前若未打开微信，会正常返回 `healthy=false` 和 `reason=未找到微信主窗口`

---

### 2026-05-27（Python RPA 兼容层瘦身）

**将旧 reader / writer / common 路径改为兼容 wrapper**

在上一轮目录收口基础上，继续瘦身 Python RPA 旧路径：

1. **旧 reader / writer 入口瘦身**
   - `readers/qianniu_reader.py` 改为委托 `platforms/qianniu/reader.py`
   - `readers/wechat_reader.py` 改为委托 `platforms/wechat/reader.py`
   - `writers/qianniu_writer.py` 改为委托 `platforms/qianniu/writer.py`
   - `writers/wechat_writer.py` 改为委托 `platforms/wechat/writer.py`
   - 保留旧路径私有函数导出，确保现有测试和调试脚本继续可用

2. **平台专用 common 模块瘦身**
   - `common/qianniu_window.py`、`qianniu_coords.py`、`qianniu_clicknium.py`
   - `common/qianniu_header.py`、`qianniu_chat_parser.py`、`qianniu_bubble_parser.py`、`qianniu_session.py`
   - `common/wechat_capture.py`、`wechat_session.py`、`wechat_uia.py`、`wechat_ocr_ops.py`
   - 上述文件均改为对应 `platforms/{platform}/` 模块的兼容 wrapper

3. **通用 common 模块瘦身**
   - `common/input_sim.py`、`screenshot.py`、`win32_window.py`
   - `common/ocr_engine.py`、`window_lock.py`、`incremental.py`、`rpa_console_log.py`
   - 上述文件均改为对应 `core/` 模块的兼容 wrapper
   - `core/layout_parser.py` 改为显式转发 `common/layout_parser.py` 的全部非 dunder 名称，兼容千牛气泡解析依赖的内部辅助函数

当前判断：
- Python RPA 的真实实现已进一步集中到 `core/`、`db/`、`platforms/`
- `common/`、`readers/`、`writers/` 仍可作为旧 import 兼容路径，但不再承载主要实现
- 后续可继续处理微信 UIA 老 reader、根目录 legacy 脚本和 `common/layout_parser.py` 的最终迁移

---

### 2026-05-27（Python RPA 目录继续收口）

**按工具、数据库、PDD 平台主线收敛 Python RPA 目录**

根据当前 Python 目录结构规范讨论，继续小步收口 `python/rpa/`：

1. **新增工具规范入口**
   - 新增 `rpa.tools._entrypoint`
   - 新增 `tools/calibration/` 下微信输入框、微信会话列表、千牛会话列表校准入口
   - 新增 `tools/preview/` 下微信 OCR 区域、微信未读扫描带、千牛未读扫描带预览入口
   - 新增 `tools/inspect/db_inspect.py`
   - 旧根目录脚本暂时保留，降低 UI 和人工命令使用风险

2. **收口 RPA DB 边界**
   - `rpa.db.connection` 承接 SQLite 默认路径解析和连接管理
   - `rpa.db.inbox_dao` 承接 inbox 写入、待发送查询、发送状态更新和发送事件记录
   - 旧 `rpa.common.db_helper` 改为兼容 wrapper，新代码建议从 `rpa.db` 导入

3. **收口 PDD 平台实现**
   - `platforms/pdd/reader.py` 承接 PDD reader 实现
   - `platforms/pdd/writer.py` 承接 PDD writer 实现
   - 旧 `readers/pdd_reader.py`、`writers/pdd_writer.py` 改为兼容 wrapper
   - `PddPlatformAdapter` 改为依赖同平台目录内 reader / writer

4. **UI 引用同步**
   - 微信 OCR 预览、微信未读扫描带预览、千牛未读扫描带预览改用 `tools/preview/` 推荐入口

当前判断：
- Python RPA 主线进一步集中到 `core/`、`db/`、`platforms/`、`tools/`
- `common/`、`readers/`、`writers/` 仍需继续作为兼容层保留，后续再逐步瘦身
- PDD 仍是截图 OCR PoC 实现，后续 M5 若切到 DOM / CDP，应优先在 `platforms/pdd/` 内替换实现

---

### 2026-05-27（docs-new 文档目录分层）

**按方向、范围、设计、执行、导航归档开发文档**

根据当前文档导航截图，对 `docs/docs-new/` 做目录分层整理：

1. **新增英文分层目录**
   - `direction/`：方向层
   - `scope/`：范围层
   - `design/`：设计层
   - `execution/`：执行层
   - `navigation/`：导航层

2. **完成文档归档**
   - 方向层：总体架构、多平台聚合、外部工具辅助接入、各平台自动化路线
   - 范围层：MVP 范围、用户场景与核心流程
   - 设计层：统一数据模型、工程骨架、平台 PoC、Python 目录重构、仓库重构方案
   - 执行层：开发任务拆分、测试验收清单、开发进度记录
   - 导航层：README、开发前文档规划建议

3. **同步维护引用**
   - 更新 `navigation/README.md` 的目录说明和阅读路径
   - 更新文档内部相对链接
   - 更新项目 skill 中的开发进度与关键文档路径

当前判断：
- `docs/docs-new/` 根目录已收敛为五个分层目录
- 后续新增文档应优先放入对应分层目录，避免根目录再次堆积

---

### 2026-05-26（Python RPA 推荐目录继续收口）

**补齐 `core/`、`db/` 与微信发现模块推荐路径**

根据 [`python目录重构方案.md`](../design/python目录重构方案.md) 的推荐目录结构，继续收口 Python RPA 目录：

1. **新增 `core/layout_parser.py` 推荐入口**
   - 新增 `rpa.core.layout_parser`
   - 平台代码改为从 `rpa.core.layout_parser` 导入 `LayoutParserConfig`、`ParsedMessage`、`parse_chat_layout`
   - 旧 `rpa.common.layout_parser` 暂时保留为实际实现与兼容路径，降低一次性迁移风险

2. **新增 `rpa/db/` 推荐入口**
   - 新增 `db/__init__.py`
   - 新增 `db/connection.py`
   - 新增 `db/inbox_dao.py`
   - 新平台层和 `main.py` 改为从 `rpa.db` 导入数据库连接、inbox 写入和发送状态 DAO
   - 旧 `common/db_helper.py` 暂时保留为实际实现与兼容路径

3. **新增微信未读发现推荐路径**
   - 新增 `platforms/wechat/reader_discover.py`
   - `platforms/wechat/reader.py` 改为从同平台目录导入 `ListOcrCache`、`execute_discover_unread_targets`、`refresh_list_cache_if_needed`
   - 旧 `readers/wechat_reader_discover.py` 暂时保留为实际实现与兼容路径

4. **删除废弃配置**
   - 删除已确认不再使用的 `python/rpa/wechat_config.json`
   - 保留有效配置：`python/rpa/config/wechat_config.json`

5. **验证**
   - Python 语法检查通过：
     - `rpa/core/layout_parser.py`
     - `rpa/db/*.py`
     - `rpa/platforms/wechat/reader_discover.py`
     - 千牛/微信平台 reader/writer
     - `rpa/main.py`
   - 推荐路径与旧兼容路径导入通过
   - 千牛、微信、PDD 平台入口导入通过
   - `tests/test_wechat_layout_parser.py` 回归通过

当前判断：
- 当前目录已更接近推荐结构，但 `core/layout_parser.py`、`db/*`、`platforms/wechat/reader_discover.py` 仍是兼容入口，实际实现尚未完全搬离旧文件
- 下一步可继续把实际实现从 `common/layout_parser.py`、`common/db_helper.py`、`readers/wechat_reader_discover.py` 搬入推荐路径，并把旧文件改为纯 wrapper

---

### 2026-05-26（M5 PoC 平台适配器）

**新增拼多多 PDD 平台适配器壳**

根据 [`python目录重构方案.md`](../design/python目录重构方案.md) 第四阶段完成 M5 PoC 平台适配器的最小落地：

1. **创建 `python/rpa/platforms/pdd/`**
   - `__init__.py`
   - `reader.py`：委托旧 `rpa.readers.pdd_reader.run_reader`
   - `writer.py`：委托旧 `rpa.writers.pdd_writer.run_writer`
   - `layout_config.py`：PDD 网页版布局解析配置
   - `adapter.py`：`PddPlatformAdapter`

2. **实现 `PddPlatformAdapter`**
   - 继承 `PlatformAdapter`
   - 实现 `start()` / `stop()` 轻量生命周期标记
   - 实现 `health_check()`，检查 PDD 浏览器窗口、最小化状态和基础元信息
   - 实现 `fetch_visible_conversations()`，返回配置中的 PoC 会话占位
   - 实现 `fetch_visible_messages()`，复用现有截图 OCR + layout parser 获取当前可见消息快照
   - 实现 `prepare_reply_draft()`，只做输入框草稿回填，不触发 Enter 发送
   - `send_message()` 仍保持基类非 MVP 默认能力，需要确认凭证和二次校验后再启用

3. **更新 PDD 主入口**
   - `rpa.main` 的 PDD reader / writer 改为从 `rpa.platforms.pdd` 导入
   - 保留旧 `readers/pdd_reader.py`、`writers/pdd_writer.py` 实现，降低迁移风险

4. **验证**
   - PDD 平台模块语法检查通过
   - `PddPlatformAdapter` / `run_reader` / `run_writer` 导入通过
   - 千牛、微信、PDD 三个平台入口导入通过
   - `rpa.main` 的 PDD 入口导入通过
   - `tests/test_wechat_layout_parser.py` 回归通过

当前判断：
- M5 PoC 已具备规范平台适配器入口，可作为后续 C++ `RpaService` / IPC 命令接入对象
- 当前 PDD 仍复用截图 OCR 链路，`metadata.observation_method=ocr_screenshot`
- 后续若切到指纹浏览器 / CDP / DOM 观测，可替换 `PddPlatformAdapter` 内部实现，不影响上层协议
- MVP 阶段仍默认草稿回填，不启用无人值守真实发送

---

### 2026-05-26（Python RPA 目录重构第三阶段）

**完成布局解析配置化抽象**

根据 [`python目录重构方案.md`](../design/python目录重构方案.md) 完成第三阶段：

1. **`layout_parser.py` 增加配置协议**
   - 新增 `LayoutParserConfig`
   - 新增 `default_layout_config()`
   - `parse_chat_layout()` 增加 `config` 参数
   - 保留原有 `platform`、`left_threshold`、`right_threshold`、`merge_y_gap` 参数，旧调用路径继续兼容

2. **平台配置下沉**
   - 新增 `platforms/qianniu/layout_config.py`
   - 新增 `platforms/wechat/layout_config.py`
   - 千牛头部正则、微信系统提示正则和默认阈值进入平台配置层
   - 平台 reader 从各自 `layout_config.py` 构建解析配置

3. **reader 接入**
   - `platforms/qianniu/reader.py` 在 fallback layout 解析时传入千牛布局配置
   - `platforms/wechat/reader.py` 在聊天区解析时传入微信布局配置
   - JSON 配置中的 `left_threshold`、`right_threshold`、`merge_y_gap` 仍然可以覆盖平台默认值

4. **验证**
   - Python 语法检查通过：
     - `rpa/common/layout_parser.py`
     - `rpa/platforms/qianniu/layout_config.py`
     - `rpa/platforms/wechat/layout_config.py`
     - `rpa/platforms/qianniu/reader.py`
     - `rpa/platforms/wechat/reader.py`
   - `tests/test_wechat_layout_parser.py` 通过
   - `rpa.platforms.qianniu` / `rpa.platforms.wechat` reader 导入通过

当前判断：
- `layout_parser.py` 已具备配置化入口，平台特化规则开始向 `platforms/*/layout_config.py` 收敛
- 为降低风险，千牛复杂切段辅助函数暂时仍保留在 `layout_parser.py`，后续如继续重构可再拆到千牛平台层
- 下一步可进入第四阶段 M5 PoC 平台适配器，或继续清理旧 `common/qianniu_*` / `common/wechat_*` 兼容文件

---

### 2026-05-26（Python RPA 目录重构第二阶段）

**完成 `python/rpa/platforms/` 平台模块归整**

根据 [`python目录重构方案.md`](../design/python目录重构方案.md) 完成第二阶段：

1. **创建 `python/rpa/platforms/` 目录结构**
   - `platforms/__init__.py`
   - `platforms/base.py` - 平台适配器基类（PlatformAdapter）
   - `platforms/qianniu/` - 千牛平台
   - `platforms/wechat/` - 微信平台

2. **千牛平台模块迁移**
   - `common/qianniu_*.py` → `platforms/qianniu/*.py`（去掉前缀）
   - `readers/qianniu_reader.py` → `platforms/qianniu/reader.py`
   - `writers/qianniu_writer.py` → `platforms/qianniu/writer.py`
   - 更新所有内部 import 路径

3. **微信平台模块迁移**
   - `common/wechat_*.py` → `platforms/wechat/*.py`（去掉前缀）
   - `readers/wechat_reader.py` → `platforms/wechat/reader.py`
   - `writers/wechat_writer.py` → `platforms/wechat/writer.py`
   - 更新所有内部 import 路径

4. **更新 main.py 入口**
   - 千牛/微信改为从 `rpa.platforms.{platform}` 导入
   - 保留拼多多的旧路径兼容

5. **兼容性保护**
   - `common/__init__.py` 添加平台模块重导出
   - 旧 `from rpa.common.qianniu_xxx import` 仍可用

6. **验证**
   - `from rpa.platforms.qianniu import run_reader, run_writer` ✅
   - `from rpa.platforms.wechat import run_reader, run_writer` ✅
   - `from rpa.common.qianniu_session import ConversationListEntry` ✅
   - `main.py` 语法检查通过 ✅

当前目录结构：

```text
python/rpa/
├── core/                    # 通用基础设施
├── platforms/               # 🆕 平台适配器
│   ├── __init__.py
│   ├── base.py              # PlatformAdapter 基类
│   ├── qianniu/
│   │   ├── __init__.py
│   │   ├── reader.py
│   │   ├── writer.py
│   │   ├── session.py
│   │   ├── window.py
│   │   ├── coords.py
│   │   ├── header.py
│   │   ├── chat_parser.py
│   │   ├── bubble_parser.py
│   │   └── clicknium.py
│   └── wechat/
│       ├── __init__.py
│       ├── reader.py
│       ├── writer.py
│       ├── session.py
│       ├── capture.py
│       ├── ocr_ops.py
│       └── uia.py
├── common/                  # 共享模块 + 兼容重导出
├── readers/                 # 旧路径（pdd 等）
├── writers/                 # 旧路径（pdd 等）
├── config/
└── main.py
```

**下一步**：第三阶段（布局解析抽象，可选）或直接进入 M5 PoC。

---

### 2026-05-26（Python RPA 目录重构第一阶段）

**完成 `python/rpa/core/` 通用基础设施模块抽取**

根据 [`python目录重构方案.md`](../design/python目录重构方案.md) 完成第一阶段：

1. **创建 `python/rpa/core/` 目录**
   - 抽取 100% 通用模块到 core/

2. **迁移的模块**
   - `input_sim.py` - Win32 输入模拟
   - `screenshot.py` - Win32 窗口截图
   - `win32_window.py` - Win32 窗口操作
   - `ocr_engine.py` - OCR 引擎抽象
   - `window_lock.py` - 平台窗口锁
   - `incremental.py` - 增量去重（使用 Protocol 解耦 ParsedMessage 依赖）
   - `rpa_console_log.py` - 日志格式化
   - `name_stabilizer.py` - OCR 名称稳定器（从 qianniu_reader/wechat_reader 合并）

3. **兼容性保护**
   - `common/__init__.py` 添加重导出，旧 import 路径仍可用
   - 新代码建议从 `rpa.core` 直接导入

4. **验证**
   - `from rpa.core import ...` 导入成功
   - `from rpa.common import ...` 兼容导入成功
   - `qianniu_reader` / `wechat_reader` / `main.py` 语法检查通过

当前目录结构：

```text
python/rpa/
├── core/                    # 🆕 通用基础设施
│   ├── __init__.py
│   ├── input_sim.py
│   ├── screenshot.py
│   ├── win32_window.py
│   ├── ocr_engine.py
│   ├── window_lock.py
│   ├── incremental.py
│   ├── rpa_console_log.py
│   └── name_stabilizer.py
├── common/                  # 平台相关 + 兼容重导出
├── readers/
├── writers/
├── config/
└── main.py
```

**下一步**：第二阶段（平台模块归整）或直接进入 M5 PoC。

---

### 2026-05-26（Python RPA 重构方案对齐主架构）

**调整 [`python目录重构方案.md`](../design/python目录重构方案.md)**

根据 [`桌面客服工作台系统_架构分析文档.md`](../direction/桌面客服工作台系统_架构分析文档.md) 收口 Python 目录重构方案：

- 明确 `python/service/` 是 Python 策略与 AI sidecar
- 明确 `python/rpa/` 是平台适配层的一种 Python / RPA 实现，不替代 C++ 主控
- 补充 `python/rpa/core/`、`python/rpa/platforms/*`、`src/ipc/`、`src/services/rpa/` 的职责边界
- 增加平台适配器协议映射，避免 PoC 接口与主架构插件协议漂移
- 增加统一事件输出建议，包括 `message_observed`、`conversation_observed`、`draft_prepared`、`send_result_observed`
- 补充审计、平台级开关、人工确认和默认草稿回填边界

当前判断：
- 该方案可作为 M5 单平台真实 PoC 的 Python/RPA 目录重构依据
- M5 应优先打通“观测消息 → 统一事件 → C++ 落盘展示 → AI 建议 → 草稿回填 → 人工确认”闭环
- 真实发送能力仍应作为非 MVP 扩展，需确认凭证和二次校验

---

### 2026-05-25（RPA 焦点隔离设想补充）

**补充桌面 RPA 两机隔离设想**

在 [`外部工具辅助平台接入方案.md`](../direction/外部工具辅助平台接入方案.md) 中补充桌面客户端 RPA 的焦点劫持风险和后期两机隔离部署设想：

- 明确影刀 / UiBot 传统模拟点击、窗口激活、快捷键可能抢占客服主机焦点
- 补充“客服主机 A + RPA 工作机 B”的后期架构设想
- 建议通过 LAN HTTP / WebSocket / gRPC 连接 Python adapter hub 与 RPA Worker
- 强调该方案当前只作为后期设想，不进入 M5 / MVP 验收
- 强调即使未来采用两机方案，也应优先只做草稿回填和状态回传
- 若未来由 RPA Worker 代执行发送，必须增加会话截图、窗口标题、账号、会话对象和待发送文本二次校验

---

### 2026-05-25（外部工具辅助接入文档收口）

**合并 Gemini 接入方案并补充主架构文档**

根据最新路线选择，完成平台接入方案文档收口：

- 合并 `不局限纯技术从零开发.md` 与 `保留自研C++聊天界面.md`
- 新增统一文档：[`外部工具辅助平台接入方案.md`](../direction/外部工具辅助平台接入方案.md)
- 明确保留两类外部工具路线：
  - 影刀 / UiBot RPA：面向千牛 PC、微信 PC 等桌面客户端
  - AdsPower / 紫鸟 / 指纹浏览器：面向拼多多、抖店等 Web 工作台
- 明确不考虑当前路线：
  - 商业客服 SaaS 旁路
  - 硬件 HID / 投屏群控
- 补充 [`多平台会话聚合_架构分析文档.md`](../direction/多平台会话聚合_架构分析文档.md)：
  - 增加外部工具辅助接入原则
  - 补充 RPA 与指纹浏览器路线
  - 扩展 `sourceType` 建议，如 `rpa_dom_observed`、`rpa_ui_observed`
  - 将 M5 推荐路线调整为“外部工具辅助单平台 PoC”

当前判断：
- C++ 聚合工作台继续作为主界面
- Python sidecar 后续应扩展为 AI + 外部工具 adapter hub
- 外部工具只做采集、草稿回填、健康检测和降级辅助，不做无人值守发送

---

### 2026-05-25（模拟消息入口优化）

**显式模拟买家消息入口**

根据体验反馈调整模拟平台入口：

- 去掉 `SimPlatformAdapter::startListening()` 启动后自动投递模拟买家消息的逻辑
- 在聚合界面最左侧工具栏新增“测”按钮
- 点击按钮后通过 `SimPlatformAdapter::simulateRandomPlatformIncomingMessage()` 模拟一条随机平台风格的买家消息
- 随机平台风格包括：千牛、拼多多、微信、抖店
- 底层仍走 `simulator` adapter，确保 MVP 演示里的人工发送、发送成功和 `/fail` 失败回执可以闭环

更新文件：
- `src/services/platforms/simplatformadapter.h/cpp`
- `src/ui/aggregatechatform.h/cpp`

验证：
- `ReadLints` 检查通过，无诊断错误

---

### 2026-05-25（待处理暂留体验优化）

**发送后会话暂留「待处理」**

优化聚合工作台发送后的分栏体验：

- 用户点击“发送”时，立即将当前会话标记为 `m_pendingStickyConvId`
- 即使最后一条消息变为出站消息，该会话也会继续暂留在「待处理」栏
- 只有客服主动切换到其它会话，或切换到其它 tab 栏时，才清除暂留标记
- 避免发送后会话立刻跳到「已回复」，导致客服当前上下文突然丢失

更新文件：
- `src/ui/aggregatechatform.cpp`

验证：
- `ReadLints` 检查通过，无诊断错误

---

### 2026-05-25（模拟平台主链路验证）

**MVP 演示链路检查与补齐**

围绕“会话列表、消息展示、AI 建议、人工发送、失败降级”做了一轮静态链路验证，并补齐模拟平台演示入口。

**1. 会话列表与消息展示**

检查链路：

```text
SimPlatformAdapter -> MessageRouter -> ConversationManager -> AggregateChatForm
```

补齐内容：
- 已调整为聚合工作台左侧“测”按钮手动投递模拟买家消息
- fresh DB 下不再需要隐藏入口，也不会在启动监听时自动污染会话数据
- 聚合工作台在当前无选中会话时收到新消息，会自动打开该会话

**2. AI 建议**

确认链路：

```text
AggregateChatForm -> IpcService -> python/service/server.py -> /api/ai/suggestion
```

当前行为：
- “生成本条回复”只请求 Python sidecar 返回建议
- 建议只填入输入框草稿
- 不自动发送，仍需客服点击“发送”

**3. 人工发送**

确认链路：

```text
输入框 -> 发送按钮 -> ConversationManager -> MessageRouter -> SimPlatformAdapter
```

当前行为：
- 发送前创建 `pending` 消息
- 模拟平台正常发送后回调 `messageSent`
- 消息状态进入 `sent`
- 会话状态进入 `waiting_customer`

**4. 失败降级**

补齐内容：
- 模拟平台支持发送失败触发：发送文本中包含 `/fail` 或 `#fail`
- 触发后 `SimPlatformAdapter` 发射 `sendFailed`
- `MessageRouter` 将消息状态更新为 `failed`
- UI 显示发送失败原因，不重复发送

**5. 验证结果**

- `ReadLints` 检查修改过的 C++ 文件：无诊断错误
- 未反复命令行构建 Qt，需在 Qt Creator 中运行验证

**Qt Creator 手动验证建议**：

1. 启动客户端并进入聚合工作台
2. 等待模拟买家消息出现，确认左侧会话列表有新会话
3. 确认中间消息区展示入站消息
4. 切换为“AI 辅助”，点击“生成本条回复”，确认输入框填入 AI 草稿
5. 点击“发送”，确认出站气泡显示已发送
6. 输入包含 `/fail` 的文本并发送，确认气泡显示发送失败和错误原因

---

### 2026-05-25（统一模型信号迁移）

**UI 层迁移到统一模型信号**

完成 M4 到 M5 之间的收口任务：

**1. MessageRouter 统一信号补齐**

更新 `src/core/messagerouter.cpp`：
- 出站发送创建 `pending` 消息后，同时发射 `unifiedMessageReceived`
- 出站发送更新会话摘要后，同时发射 `unifiedConversationUpdated`
- 发送确认和发送失败更新会话时，也同步发射 `unifiedConversationUpdated`
- 保留旧 `messageSentOk`、`conversationUpdated` 信号用于兼容

**2. ConversationManager 转发统一模型信号**

更新 `src/core/conversationmanager.h/cpp`：
- 新增 `unifiedMessageReceived(int, const Models::Message&)`
- 新增 `unifiedConversationUpdated(const Models::Conversation&)`
- 连接并转发 `MessageRouter` 的统一模型信号
- 保留旧信号给尚未迁移的兼容路径

**3. AggregateChatForm 改接统一信号**

更新 `src/ui/aggregatechatform.h/cpp`：
- 聚合工作台不再直接监听旧 `newMessageReceived` / `messageSentOk`
- 改为监听 `ConversationManager::unifiedMessageReceived`
- 改为监听 `ConversationManager::unifiedConversationUpdated`
- 在 UI 边界临时将 `Models::Message` / `Models::Conversation` 转为现有 `MessageRecord` / `ConversationInfo`，避免一次性重写现有 Qt delegate 和 list model

**4. 元类型注册**

更新 `src/main.cpp`：
- 注册 `Models::Message`
- 注册 `Models::Conversation`

**5. 验证**

- `ReadLints` 检查修改过的 C++ 文件：无诊断错误

**后续注意**：

- 当前是“信号入口迁移”，UI 内部仍复用旧 `MessageRecord` / `ConversationInfo` 渲染模型
- 后续如果继续收敛，可将 `ConversationListModel` / `MessageListModel` 的内部数据源进一步迁到 `Models::*`

---

### 2026-05-25（M4 接入）

**聚合工作台接入 Python AI sidecar**

完成 M4 主链路接入：

**1. IpcService 增强**

更新 `src/ipc/ipcservice.h/cpp`：
- 新增 `startManagedService()`、`stopManagedService()`、`ensureServiceAvailable()`
- 新增 `isManagedServiceRunning()`
- 使用 `QProcess` 启动 `python -m service.server --host 127.0.0.1 --port 8765`
- 启动前优先探测外部已运行的 `/api/health`
- 应用退出时通过 `IpcService::shutdown()` 停止托管 sidecar
- 健康检查超时收紧到 1 秒，避免 UI 长时间卡住

**2. 聚合工作台 AI 建议入口改走 IPC**

更新 `src/ui/aggregatechatform.h/cpp`：
- “生成本条回复”不再直接创建流式模型会话，而是构造 `Ipc::AiSuggestionRequest`
- 请求内容包含最近 8 条会话文本、平台类型、客户上下文、模型菜单 key
- 点击生成时自动确保 Python sidecar 可用
- 接收 `aiSuggestionReceived` 后将第一条建议填入输入框草稿
- 请求失败、超时、取消时恢复 UI 状态并显示中文提示
- AI 建议仍只填入输入框，不自动发送

**3. 应用退出管理**

更新 `src/main.cpp`：
- 注册 `Ipc::AiSuggestionResponse` 元类型
- `aboutToQuit` 时调用 `IpcService::shutdown()`，确保托管 sidecar 被关闭

**4. 验证**

- `ReadLints` 检查修改过的 C++ 文件：无诊断错误
- Python sidecar 语法检查通过：
  - `python -m py_compile python/service/__init__.py python/service/ai_suggestion.py python/service/server.py`

**当前 M4 状态**：

- Python sidecar 端点：完成
- C++ IPC 请求/响应：完成
- 聚合工作台 AI 建议入口：完成
- sidecar 启停管理：完成
- 后续可将模板兜底升级为真实模型调用，或继续推进单平台 PoC

---

### 2026-05-25（M4 启动）

**Python AI sidecar 服务骨架**

开始 M4 阶段，先补齐 Python 端 IPC 服务：

**1. 新增 `python/service/`**

新增文件：
- `python/service/__init__.py`
- `python/service/server.py`
- `python/service/ai_suggestion.py`
- `python/service/README.md`
- `python/service/run_service.bat`

**2. 对齐 C++ `IpcService` 协议**

已实现接口：
- `GET /api/health`
  - 返回 `healthy`、`version`、`service`
- `POST /api/ai/suggestion`
  - 接收 `request_id`、`conversation_id`、`platform`、`max_suggestions`、`customer_context`、`messages`、`metadata`
  - 返回 `status`、`suggestions`、`metadata`

当前 Python 服务使用标准库 `http.server`，不新增 FastAPI 等依赖，便于先验证 C++ / Python IPC 主链路。

**3. AI 建议策略**

当前实现为模板兜底：
- 根据最近一条客户消息生成 1-3 条中文客服回复建议
- 对退款、投诉、金额、隐私等关键词打 `risk_flags`
- 不自动发送，不改变消息状态

**4. 验证**

已完成轻量验证：
- `python -m py_compile python/service/__init__.py python/service/ai_suggestion.py python/service/server.py`
- 使用 `build_ai_suggestion_response()` 做本地冒烟测试，返回 `success` 和 3 条建议

**待继续**：
- 将聚合工作台的 AI 建议入口接到 `IpcService`
- 启动/停止 Python sidecar 的进程管理
- 根据真实模型配置决定是否从模板兜底升级为模型调用

---

### 2026-05-25（M3 收尾）

**状态机与本地缓存补齐**

完成 M3 阶段剩余任务：

**1. SQLite 本地缓存补齐**

更新 `src/data/database.cpp`：
- 新增 `conversation_drafts` 表，用于保存每个会话的输入草稿
- 新增 `app_state` 表，用于保存客户端最近选择会话等轻量状态
- 将新建会话默认状态调整为 `new`

**2. ConversationDao 增强**

更新 `src/data/conversationdao.h/cpp`：
- 新增 `draftForConversation()`、`saveDraft()`、`clearDraft()`
- 新增 `lastSelectedConversationId()`、`setLastSelectedConversationId()`
- 新建会话状态从旧的 `open` 收敛为 `new`
- `Models::Conversation` 创建路径直接写入统一状态字符串

**3. 会话状态流转**

更新 `src/core/conversationmanager.cpp` 与 `src/core/messagerouter.cpp`：
- 选择会话时：状态更新为 `active`，清除未读并保存最近会话
- 收到客户入站消息时：状态更新为 `waiting_agent`
- 客服发送出站消息后：状态更新为 `waiting_customer`
- 关闭会话时：状态更新为 `closed`，并清理最近选择会话
- 已关闭会话再次收到消息时：先恢复为 `active`，后续进入入站流转

**4. 输入草稿保存与恢复**

更新 `src/ui/aggregatechatform.h/cpp`：
- 输入框内容变化后通过 600ms 防抖写入 SQLite
- 切换会话前立即保存当前草稿
- 打开会话时恢复该会话草稿
- 发送成功触发后清空该会话草稿
- 客户端进入聚合工作台后恢复最近选择会话；若不可见则选择当前列表第一条

**当前 M3 状态**：

- 消息状态流转：完成
- 会话状态流转：完成
- 最近会话和消息：沿现有 `conversations/messages` 持久化恢复
- 输入草稿：完成
- 最近选择会话：完成

---

### 2026-05-25（下午续）

**消息状态流转实现**

完成了 M3 阶段的核心任务——消息状态流转：

**1. MessageListModel 扩展**

更新 `src/ui/messagelistmodel.h/cpp`：
- 新增 `updateMessageStatus(int messageId, Models::MessageStatus, QString errorReason)` 方法
- 新增 `updateMessageById(int messageId, MessageRecord)` 方法
- 新增 `findRowByMessageId()` 辅助方法
- 新增 `MessageStatusRole` 角色
- 新增 `messageStatusChanged` 信号
- 状态变化时触发 `dataChanged` 通知 UI 刷新

**2. MessageRouter 状态信号**

更新 `src/core/messagerouter.h/cpp`：
- 新增信号 `messageStatusChanged(int conversationId, int messageId, MessageStatus, QString errorReason)`
- 在 `onMessageSent()` 中发射状态变化（Pending → Sent）
- 在 `onSendFailed()` 中发射状态变化（Pending → Failed）

**3. ConversationManager 信号转发**

更新 `src/core/conversationmanager.h/cpp`：
- 新增信号 `messageStatusChanged`
- 在 `initialize()` 中连接 MessageRouter 的状态信号

**4. AggregateChatForm 状态响应**

更新 `src/ui/aggregatechatform.h/cpp`：
- 新增槽函数 `onMessageStatusChanged()`
- 在 `connectSignals()` 中连接 ConversationManager 的状态信号
- 收到状态变化时调用 `MessageListModel::updateMessageStatus()` 更新 UI

**5. 消息气泡状态展示优化**

更新 `ModernMessageItemDelegate::paintSendStatus()`：
- `syncStatus=10` (Pending): 显示"发送中..." + 黄色 (#FBBF24)
- `syncStatus=11` (Sent): 显示"已发送" + 浅蓝色 (#E0F2FE)
- `syncStatus=12` (Failed): 显示"发送失败" + 浅红色 (#FCA5A5)
- 失败时额外显示错误原因（红色 #DC2626）

**状态流转链路**：

```
用户点击发送
    ↓
MessageRouter.sendMessage()
    ↓ 创建消息，状态=Pending (syncStatus=10)
MessageDao.create()
    ↓
IPlatformAdapter.sendMessage()
    ↓
适配器回调 onMessageSent/onSendFailed
    ↓
MessageRouter 发射 messageStatusChanged
    ↓
ConversationManager 转发信号
    ↓
AggregateChatForm.onMessageStatusChanged()
    ↓
MessageListModel.updateMessageStatus()
    ↓ 触发 dataChanged
QListView 重绘气泡，显示新状态
```

---

### 2026-05-25（下午）

**模块边界重构**

完成了架构文档中规划的模块边界重构工作：

**1. 创建 IPC 通信层 (`src/ipc/`)**

新增文件：
- `ipctypes.h/cpp` - 定义 IPC 请求/响应类型
- `ipcservice.h/cpp` - 实现 C++ 与 Python 服务通信

核心类型：
- `Ipc::AiSuggestionRequest/Response` - AI 建议请求响应
- `Ipc::RpaCommandRequest/Response` - RPA 命令请求响应
- `Ipc::HealthCheckRequest/Response` - 健康检查
- `Ipc::IpcService` - 单例服务，封装 HTTP 通信、超时、请求 ID 关联

**2. 创建 RPA 服务层 (`src/services/rpa/`)**

新增文件：
- `rpatypes.h/cpp` - RPA 类型定义（`PlatformId`、`ProcessState`、`PlatformStatus`）
- `rpaprocessmanager.h/cpp` - Python RPA 进程生命周期管理
- `rpaservice.h/cpp` - 高层服务接口

核心改进：
- UI 层 `RpaProcessController` 简化为代理，不再直接管理进程
- 进程管理逻辑完全从 UI 层剥离到 services 层
- 支持平台状态变更通知、日志事件、错误处理

**3. 收敛 MessageRouter**

更新 `src/core/messagerouter.h/cpp`：
- 新增统一模型信号：
  - `unifiedMessageReceived(int, const Models::Message&)`
  - `unifiedConversationUpdated(const Models::Conversation&)`
  - `conversationEventDispatched(const Models::ConversationEvent&)`
- 新增 `dispatchEvent()` 方法
- 内部逻辑使用统一模型，保留旧信号向后兼容

**4. 更新 CMakeLists.txt**

添加所有新模块的源文件和头文件。

**新目录结构**：

```
src/
├── ipc/                    # 新增：IPC 通信层
├── services/
│   ├── rpa/                # 新增：RPA 服务层
│   ├── ai/                 # 已有
│   ├── app/                # 已有
│   └── platforms/          # 已有
├── core/                   # 已更新 MessageRouter
├── models/                 # 已有
├── data/                   # 已有
├── ui/                     # 已简化 RpaProcessController
└── utils/                  # 已有
```

---

### 2026-05-25（上午）

**文档整理与 Skill 创建**

- 阅读并理解了 `docs/docs-new/` 下的架构文档
- 创建了项目专属开发规范 skill：`.cursor/skills/ai-customer-service-dev/`
- 创建了本进度文档

**仓库现状确认**：

- 统一模型（`src/models/unifiedmodels.h`）已建立：
  - `Models::Conversation`、`Message`、`PlatformAccount`
  - `Models::ConversationEvent`、`SendMessageCommand`、`AISuggestion`
  - 枚举：`PlatformType`、`SourceType`、`VerificationStatus` 等
- 旧模型兼容层（`src/core/types.h`）已保留，提供 `LegacyModelCompat` 转换
- Qt Model/View 已收敛：
  - `ConversationListModel` + `QListView` + delegate
  - `MessageListModel` + `QListView` + delegate
- 平台适配器接口已定义（`IPlatformAdapter`）
- AI 服务层较完整（`src/services/ai/`）

**待完成事项**：

1. 完善消息状态流转
2. 加入本地 SQLite 草稿和会话恢复
3. ~~抽离 IPC 边界~~ ✅ 已完成
4. ~~收敛 MessageRouter~~ ✅ 已完成
5. ~~把 UI 中的 RPA 控制逻辑剥离~~ ✅ 已完成

---

## 已知问题

1. ~~UI 层仍有较多业务逻辑，需要继续抽薄~~ → RPA 控制已剥离，其他待继续
2. ~~Python RPA 与主程序共享数据库，边界不够硬~~ → RPA 服务层已建立
3. 构建验证受 Visual Studio 锁文件影响，建议在 Qt Creator 或开发者命令提示符中构建
4. 编译需要正确的 MSVC 环境（Visual Studio 开发者命令提示符）

---

## 下一步任务

按优先级排序：

1. ~~**M3 状态机与缓存（续）**~~ ✅ 基本完成
   - ~~实现消息状态流转（draft → pending → sent/failed）~~ ✅ 已完成
   - ~~保存最近会话和草稿到 SQLite~~ ✅ 已完成
   - ~~应用重启后恢复基础数据~~ ✅ 已完成
   - ~~实现会话状态流转（new → active → waiting_customer → closed）~~ ✅ 已完成

2. ~~**Python AI 服务实现**~~ ✅ 基本完成
   - ~~实现 `/api/ai/suggestion` 端点~~ ✅ 已完成
   - ~~实现 `/api/health` 端点~~ ✅ 已完成
   - ~~与 IpcService 对接测试~~ ✅ 已完成
   - ~~将聚合工作台 AI 建议入口接入 Python sidecar~~ ✅ 已完成

3. ~~**UI 层迁移到统一模型信号**~~ ✅ 已完成入口迁移
   - ~~使用 `unifiedMessageReceived` 替代旧信号~~ ✅ 已完成
   - ~~使用 `unifiedConversationUpdated` 替代旧信号~~ ✅ 已完成
   - 后续可继续将 UI model 内部数据源迁到 `Models::*`

4. ~~**IPC 边界抽离**~~ ✅ 已完成
   - ~~建立独立的 `ipc` 模块~~
   - ~~统一 C++ 与 Python 通信协议~~

5. ~~**MessageRouter 收敛**~~ ✅ 已完成
   - ~~让 MessageRouter 成为事件归一与分发中心~~
   - ~~移除 UI 对平台实现的直接依赖~~

---

## 里程碑检查点

### MVP 完成标准

- [x] Qt 客户端可运行
- [x] 统一模型已建立
- [x] IPC 通信层已建立
- [x] RPA 服务层已建立
- [x] MessageRouter 已收敛
- [x] 消息状态流转完整（Pending → Sent/Failed）
- [x] 会话状态流转完整
- [x] 本地缓存可恢复（草稿、最近会话、最近消息）
- [x] Python AI sidecar 端点已实现
- [x] Python AI 建议可用
- [x] UI 实时消息/会话入口已迁移到统一模型信号
- [x] 模拟平台主链路完整
- [ ] 单平台真实 PoC 可演示
- [ ] 人工确认发送边界清晰
- [x] 失败可降级，不误发（模拟链路）

### 模块边界重构检查点（已完成）

- [x] `src/ipc/` - IPC 通信层
  - [x] `IpcService` 单例
  - [x] AI 建议请求/响应类型
  - [x] 健康检查接口
  - [x] 超时和取消机制
- [x] `src/services/rpa/` - RPA 服务层
  - [x] `RpaProcessManager` 进程管理
  - [x] `RpaService` 高层接口
  - [x] UI 层 `RpaProcessController` 简化为代理
- [x] `src/core/messagerouter.*` - 收敛
  - [x] 统一模型信号
  - [x] 事件分发方法
  - [x] 保持旧信号兼容

---

### 2026-05-29（微信旧 OCR 默认入口下线）

本轮进一步收口微信 RPA 主路径：

- Python 微信自动化默认入口只保留 `python/service/rpa_bridge.py` -> `python/rpa/platforms/wechat/*` sidecar 路线。
- 删除旧微信 OCR reader/writer/session/capture/preview/calibration/workbench Python 文件后，`rpa.common` 不再重导出 `wechat_capture`、`wechat_session`、`wechat_ocr_ops` 等旧兼容包装。
- C++ `MainWindow` 不再提供微信 OCR 校准入口，也不再在首页暴露“微信 RPA 工作台”快捷入口。
- `WeChatWorkbenchService` 不再尝试启动已删除的 `python/rpa/tools/wechat_workbench_service.py`，旧工作台 Python 服务明确下线。
- 千牛 OCR 校准和拼多多 UI/窗口识别入口保留；本轮删除的是微信旧 OCR 自动化实现和默认入口。

验证：已通过微信 sidecar 相关 Python AST 检查，以及 `test_wechat_uia_scoring.py`、`test_wechat_role_and_send.py`、`test_wechat_session_v2.py`。

---

### 2026-06-02（千牛 sidecar 接入 bridge）

本轮先打通 Python sidecar 的第二个平台接入，不扩展 HTTP 协议，只补内部平台分发和千牛 adapter：

- 新增 `python/rpa/platforms/qianniu/adapter.py`
- 新增 `python/rpa/platforms/qianniu/__init__.py`
- `QianniuSender` 新增 `prepare_reply_draft()`
- `python/service/rpa_bridge.py` 从只支持 `wechat` 改为支持 `wechat` / `qianniu`

当前千牛 sidecar 已支持的统一命令：

- `connect`
- `disconnect`
- `health_check`
- `fetch_visible_conversations`
- `fetch_visible_messages`
- `scan_unread_and_fetch`
- `prepare_reply_draft`
- `send_message`

当前判断：

- 千牛底层能力已通过 sidecar adapter 接到统一 bridge，`/api/rpa/command` 和 `/api/rpa/health` 只要带 `platform=qianniu` 即可走千牛分发
- 当前是“命令式接入”而不是“持续 observer 接入”，先满足单平台验证和受控调用
- 下一步优先补千牛命令行验证入口，或继续把千牛接到 C++ 平台适配器

验证：

- 已通过 `python -m py_compile` 检查：
  - `rpa/platforms/qianniu/adapter.py`
  - `rpa/platforms/qianniu/sender.py`
  - `service/rpa_bridge.py`
- 已通过单测：
  - `tests/test_qianniu_sidecar_adapter.py`
  - `tests/test_rpa_bridge_platform_routing.py`
- 已做本地 bridge 冒烟验证，`qianniu` 的 `health_check` 可返回结构化结果
### 2026-05-29��΢�������ſ��뷢�Ͳ���������
- ��¼��ų�ʼ��΢��ƽ̨������������ͻ�������������΢�� sidecar��
- ΢�ŷ��Ͳ������� prefer_background / allow_foreground_fallback������Ĭ��ǰ̨�����ս�������ʽ����ʱ���á�
- ΢�Ųݸ�������ȳ��� UIA ValuePattern��ʧ�ܺ������������ʱ�л���ǰ̨����ͼ����塣

### 2026-05-29��΢����չ����
- ���� `wechat_conversations` �� `wechat_messages` ����΢����չ����
- ͳһ `messages` / `conversations` ������Ϊ�ۺ�������΢����չ��ֻ�н�΢��ԭʼ payload����ɫ�жϺ�����ֶΡ�
- ΢����վ��Ϣ��˳��д����չ����ɾ���Ựʱ������΢����չ��¼��
